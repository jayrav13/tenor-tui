# README Redesign Implementation Plan

> **For agentic workers:** This is a documentation-only task. No TDD cycle — write content, verify formatting, commit.

**Goal:** Replace the minimal README.md with a comprehensive guide covering installation, configuration, usage, keybindings, provider details, development setup, and the Claude-first development model.

**Architecture:** Single file rewrite of `README.md` at repo root. Content sourced from existing code (help_overlay.py keybindings, config.py options, app.py bindings, pyproject.toml deps).

**Tech Stack:** Markdown

**Spec:** `docs/superpowers/specs/2026-03-21-readme-design.md`
**Issue:** #16

---

### Task 1: Write the README

**Files:**
- Modify: `README.md`

**Source files to reference (already read, content known):**
- `src/tenortui/widgets/help_overlay.py` — canonical keybindings list
- `src/tenortui/app.py:27-32` — app-level BINDINGS + on_key handler for vim keys
- `src/tenortui/app.py:160-174` — command palette commands
- `src/tenortui/config.py` — config paths, provider required fields, YAML structure
- `src/tenortui/providers/base.py` — DataProvider protocol
- `pyproject.toml` — Python version, dependencies, entry point

- [ ] **Step 1: Write README content**

Structure per spec (8 sections):

1. **Header & Tagline** — "TenorTUI" + one-liner + 2-3 sentence pitch (data-source agnostic, vim-native, terminal)

2. **Features** — 4 bullets:
   - Pluggable data providers (Yahoo default, Tradier with Greeks)
   - Vim-style navigation (j/k/g/G/h/l) + command palette
   - ATM strike auto-detection with viewport centering
   - Recently viewed tickers with live quotes

3. **Quick Start** — Prerequisites (Python 3.11+, git), then:
   ```
   git clone <repo>
   cd tenor-tui
   pip install .
   tenortui
   ```
   Note: Yahoo Finance works with zero config.

4. **Configuration** — `~/.config/tenor/config.yaml` location, legacy `~/.tenorrc` fallback. Full example config with comments. Yahoo section (no config needed, no Greeks). Tradier section (api_key required, sandbox optional, provides Greeks). CLI override: `--provider tradier`.

5. **Keybindings** — Three tables matching help_overlay.py sections: Navigation (j/k, h/l, g/G, Tab/Shift+Tab), Actions (/ or s, r, Enter, q), Panels (?, :). Then command palette commands (`:quit`, `:search <SYMBOL>`, `:help`).

6. **Data Providers** — Comparison table (Provider | Auth | Greeks). Brief mention of provider protocol in `providers/base.py` with the three methods to implement: `get_quote()`, `get_expirations()`, `get_chain()`.

7. **Development** — venv + `pip install -e ".[dev]"`. Commands: pytest, ruff check, ruff format. Brief architecture: app flow, widget hierarchy, provider pattern (sync methods + asyncio.to_thread). Point to CLAUDE.md.

8. **Built with Claude** — Claude-first project, parallel worktree development, structured issue→branch→PR→merge workflow, CLAUDE.md as canonical dev guide, contributions welcome.

- [ ] **Step 2: Verify README renders correctly**

Run: visual scan of markdown structure — confirm no broken tables, code blocks, or heading hierarchy issues.

- [ ] **Step 3: Commit**

```bash
git add README.md docs/
git commit -m "docs: comprehensive README with install, config, usage, and dev guide

Closes #16

Co-Authored-By: Claude <noreply@anthropic.com>"
```
